import { Fragment, useState } from 'react'
import { useSelector } from 'react-redux'
import AddedRolesList from './components/AddedRolesList'

const FunctionRoles = ({}) => {
  return (
    <Fragment>
      <AddedRolesList />
    </Fragment>
  )
}

export default FunctionRoles
